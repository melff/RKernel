define(['base/js/namespace'], ({
	notebook,
	actions,
	keyboard_manager,
	pager,
}) => ({
	onload() {
            // No op for now ...
            // console.info('Javascript from RKernel loaded');
	},
}))

/*
define(function(){
  return {onload: function(){
    console.info('Kernel specific javascript loaded');
  }}
});
*/
